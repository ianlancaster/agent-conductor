---
'agent-conductor': patch
---

Batch iTerm liveness observations per reconciliation cycle so session count no longer multiplies
AppleScript tree traversals, while preserving scalar backend compatibility and status output.
