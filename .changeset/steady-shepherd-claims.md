---
'agent-conductor': minor
---

Add an opt-in persistent PR Shepherd lane with idempotent local claim and unclaim controls,
caller-attributed audit evidence, direct polling independent of GitHub review assignment, and
authored-lifecycle compatibility without changing the configured GitHub identity.
