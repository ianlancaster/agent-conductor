---
'agent-conductor': patch
---

Allow stock CLI fleets to register additional coding runtimes through explicit trusted local,
versioned SessionRuntime factories. Keep validation non-executing, preserve built-ins and expose
registered runtime names through existing lifecycle, MCP and help surfaces.
