---
'agent-conductor': minor
---

Add configurable Git-backed spawn templates, including a default `agent` template, and expose selection through
`/spawn --template` (`-t`) and `spawn_session.template`.
