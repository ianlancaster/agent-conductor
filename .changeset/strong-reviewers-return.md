---
'agent-conductor': minor
---

Allow `spawn_session` and `/spawn -s|--session-id` to recreate a workspace while resuming one exact native runtime conversation, and make teardown retention of conversation data an explicit contract.
