---
'agent-conductor': minor
---

Add an opt-in exact-head release gate for persistently tracked pull requests, with idempotent local
attest/revoke controls, durable audit evidence, conditional direct merge or merge-queue submission,
and restart-safe queue compensation without changing ungated Shepherd behavior.
