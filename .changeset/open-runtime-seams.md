---
'agent-conductor': minor
---

Open the embedding contract for injected session runtimes, validate all configured runtime names against the final registry, expose runtime-aware lifecycle schemas and help, and define an explicit package export boundary with a complete external-adapter guide and runnable host.
