---
'agent-conductor': patch
---

Cancel queued local messages when the Conductor restarts instead of replaying stale conversation, keep explicit idempotent retries available, and fix iTerm's Unicode pane snapshot comparison so protected delivery can reach idle panes.
