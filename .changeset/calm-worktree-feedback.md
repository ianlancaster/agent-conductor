---
'agent-conductor': patch
---

Detect externally-created linked worktrees in the Claude Code status line, expose session paths
and branches through fleet status, and document worktree destination, branch-base, and setup
semantics.
