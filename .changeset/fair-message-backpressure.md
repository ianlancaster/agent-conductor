---
'agent-conductor': minor
---

Bound durable per-recipient message queues with a configurable default capacity of five, atomic
direct and broadcast admission, and actionable `queue_full` results across local, operator,
integration, and federated send paths.
