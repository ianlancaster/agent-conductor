---
'agent-conductor': minor
---

Queue peer messages durably while recipients are paused, recover pending delivery after restart, and add snapshot-based `all` and federation-wide pause/resume shortcuts without introducing a second pause mode.
