---
'agent-conductor': minor
---

Add durable agent work-status claims, blocker resolution, operator acceptance and rejection,
work closure, and an attention-sorted status view. Claude Code notifications with missing,
unknown, or non-prompt types no longer route a blocked stall; `idle_prompt` uses confirmed
idle routing.
