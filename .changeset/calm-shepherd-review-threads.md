---
'agent-conductor': minor
---

Enrich owned pull-request review feedback with coalesced inline-thread creation, reply, outdated,
and resolved transitions, including durable restart-safe cursors, self/ignored-actor suppression,
and byte-bounded routing context with explicit truncation signals for profile-authored and
explicitly tracked pull requests.
