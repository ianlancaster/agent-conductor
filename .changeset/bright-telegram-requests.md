---
'agent-conductor': minor
---

Add the opt-in Telegram operator adapter configuration, fleet `.conductor/.env` resolution, and
channel-neutral selectable operator requests with inline Telegram buttons and console
fallback commands.
