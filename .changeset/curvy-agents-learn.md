---
'agent-conductor': minor
---

Ship a topic-based agent handbook, expose it through the session-only `get_conductor_docs` tool,
and point every injected Conductor protocol at the lazy reference and authoritative fleet paths.
Add a complete Telegram adapter setup guide.
