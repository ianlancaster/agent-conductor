---
'agent-conductor': minor
---

Let the stock Conductor CLI load explicitly configured trusted local `ConductorIntegration`
factory files, with non-executing validation and foreground-owned lifecycle.
