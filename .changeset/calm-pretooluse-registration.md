---
'agent-conductor': minor
---

Add explicit fleet and per-session Claude Code `PreToolUse` command registration with strict
validation, replacement/opt-out semantics, launch-time rendered digests, and status that keeps
declaration, rendering, and runtime registration evidence separate.
