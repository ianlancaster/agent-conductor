---
'agent-conductor': patch
---

Keep Claude Code sessions working when queued input pushes the pulsing activity row outside the
captured pane and Claude displays its queued-message editing hint instead.
