# OpenCodex manual shakedown

Use the packaged [OpenCodex runtime acceptance guide](../../guides/opencodex-acceptance.md).
It covers both optional CLI substrates, native child-model selection, failure recovery, and
Conductor messaging without assuming credentials are available in CI.
